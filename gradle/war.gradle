apply plugin: "war"

bootWar {
    mainClassName = "vn.goline.cp24.api.DrawApiApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
